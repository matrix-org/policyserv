# policyserv library

This package contains functions and code that can be imported by other projects for interacting with policyserv. It is
"public API", meaning that the function and type interfaces are stable. No other packages in policyserv are stable.
